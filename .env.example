# Copy to .env and fill in real values. Used only by Claude's own dev/test scripts —
# the live userscript stores its own copy of these via its in-app Settings panel, not this file.

NOTION_API_KEY=
NOTION_CONTACTS_DB_ID=
NOTION_TASKS_DB_ID=
